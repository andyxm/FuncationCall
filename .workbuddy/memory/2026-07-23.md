# 2026-07-23 跟进

## 关键修正
- 发现 `assistant_ticket_bot-1.py` 第107行连接串仍是 `mysql+mysqlconnector://`（昨日"改为 pymysql"的 Edit 未真正落盘/被还原），导致运行会再次报 `No module named 'mysql'`。
- 已改回 `mysql+pymysql://`，语法校验通过。
- 端到端验证：用脚本完全一致逻辑（pymysql + `text()` 包裹 + `to_markdown`）实跑 `DATE_FORMAT(...,'%Y-%m-%u')` SQL，返回 15 行真实数据，表格正常。

## 结论：代码层面所有问题已闭环
根因链（按出现顺序）：
1. venv 依赖缺失/版本错配（modelscope_studio legacy 模块、gradio）→ 装 `qwen_agent[gui]==0.0.34`
2. DashScope 免费额度耗尽 → 用户充值/换 key（代码无关）
3. 模型不调用工具（qwen-max）/ 旧进程未重启 → 改 `qwen-max` + 强制 prompt + 重启
4. `No module named 'mysql'` → 改用 pymysql 驱动
5. `JSONDecodeError` → parameters 改标准 JSON Schema 字典 + call 容错正则兜底
6. `Failed raising error` 真因 = pymysql `%` 转义坑：`pd.read_sql` 传空参数元组使 `%Y` 被当占位符 → 用 `text()` 包裹；装 `tabulate`；错误提示解包 `e.orig` 露出真错误

## 用户侧最终操作
`taskkill /F /IM python.exe` → 重新 `python assistant_ticket_bot-1.py` → UI 提问即可看到 SQL 块 + 真实结果表格，`sql_calls.log` 写入 SQL。
